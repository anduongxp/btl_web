<!DOCTYPE html>
<html lang="en">
<head>
    <title>Setting</title>
    <link href="/styleSetting/style.css" type="text/css" rel="stylesheet">

    <style>
        .comic-list {
          display: flex;
          flex-wrap: wrap;
          justify-content: space-around;
        }
        .comic-card {
            flex-basis: calc(20% - 20px); /* 20% để có 5 thẻ trên mỗi dòng, trừ đi 20px margin */
            height: 300px;
            background-color: #f0f0f0;
            border-radius: 10px;
            margin: 10px;
            padding: 10px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease;
        }
        .comic-card:hover {
            transform: scale(1.05);
        }
        .comic-img {
            width: 100%;
            height: auto;
            border-radius: 5px;
        }
        .comic-card .description {
            color: black;
        }
      </style>
    <%- include("../patials/head"); %>
</head>
<body>
    <%- include('../patials/header'); %>
    <main>
        <div class="container_setting">
            <%- include('../patials/leftNavbar'); %>
            <div class="main_content">
                Story favorite
                <div class="comic-list">
                    <% for (let i = 0; i < ListStory.length; i++) { %>
                    <div class="comic-card">
                       <a href="/detail/<%= ListStory[i].id %>">
                        <img src="https://images.pexels.com/photos/236047/pexels-photo-236047.jpeg?cs=srgb&dl=clouds-cloudy-countryside-236047.jpg&fm=jpg" alt="Comic 1" class="comic-img">
                        <h3><%= ListStory[i].title %></h3>
                        <p class="description"><%= ListStory[i].Description %></p>
                       </a>
                    </div>
                    <% } %>
                </div>
            </div>
        </div>
    </main>
</body>
</html>